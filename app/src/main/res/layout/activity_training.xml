<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">
            <Button
                android:id="@+id/backButton"
                android:layout_width="48dp"
                android:layout_height="44dp"
                android:background="@drawable/bg_button_secondary"
                android:text="‹"
                android:textColor="@color/text_primary"
                android:textSize="26sp" />
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:layout_weight="1"
                android:text="Entrenamiento YOLO26"
                android:textColor="@color/text_primary"
                android:textSize="22sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/datasetText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_card"
            android:padding="14dp"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="El teléfono prepara y envía el dataset, inicia el trabajo, muestra el progreso y descarga el mejor modelo. El cálculo completo de pesos YOLO26 requiere un servidor Python/GPU configurable; no se simula como entrenamiento local."
            android:textColor="@color/text_secondary"
            android:textSize="12sp" />

        <TextView style="@style/FieldLabel" android:text="URL del servidor de entrenamiento" />
        <EditText
            android:id="@+id/endpointInput"
            style="@style/FieldInput"
            android:hint="http://192.168.1.20:8000"
            android:inputType="textUri"
            android:singleLine="true" />

        <TextView style="@style/FieldLabel" android:text="Token API (opcional)" />
        <EditText
            android:id="@+id/tokenInput"
            style="@style/FieldInput"
            android:hint="Token del servidor"
            android:inputType="textPassword"
            android:singleLine="true" />

        <TextView style="@style/FieldLabel" android:text="Épocas" />
        <EditText
            android:id="@+id/epochsInput"
            style="@style/FieldInput"
            android:inputType="number"
            android:singleLine="true" />

        <Switch
            android:id="@+id/autoCurationSwitch"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="10dp"
            android:text="Reevaluar y depurar anotaciones con el mejor modelo"
            android:textColor="@color/text_primary"
            android:thumbTint="@color/primary" />

        <Switch
            android:id="@+id/purgeRejectedSwitch"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Eliminar definitivamente cajas de baja confianza"
            android:textColor="@color/danger"
            android:thumbTint="@color/danger" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Desactivado por defecto: si no se eliminan, se excluyen del entrenamiento pero siguen disponibles para corregirlas."
            android:textColor="@color/text_secondary"
            android:textSize="11sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <EditText
                android:id="@+id/acceptInput"
                style="@style/FieldInput"
                android:layout_width="0dp"
                android:layout_marginEnd="4dp"
                android:layout_weight="1"
                android:hint="Aceptar %"
                android:inputType="numberDecimal" />
            <EditText
                android:id="@+id/rejectInput"
                style="@style/FieldInput"
                android:layout_width="0dp"
                android:layout_marginStart="4dp"
                android:layout_weight="1"
                android:hint="Rechazar bajo %"
                android:inputType="numberDecimal" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="14dp"
            android:orientation="horizontal">
            <Button
                android:id="@+id/testButton"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_margin="3dp"
                android:layout_weight="1"
                android:background="@drawable/bg_button_secondary"
                android:text="Probar servidor"
                android:textAllCaps="false"
                android:textColor="@color/text_primary" />
            <Button
                android:id="@+id/startButton"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_margin="3dp"
                android:layout_weight="1"
                android:background="@drawable/bg_button_primary"
                android:text="Iniciar entrenamiento"
                android:textAllCaps="false"
                android:textColor="@color/background"
                android:textStyle="bold" />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/trainingProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="12dp"
            android:layout_marginTop="16dp"
            android:max="1000"
            android:progressTint="@color/primary" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_card"
            android:minHeight="76dp"
            android:padding="14dp"
            android:text="Sin trabajo activo"
            android:textColor="@color/text_secondary"
            android:textSize="13sp" />

        <Button
            android:id="@+id/downloadButton"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_button_primary"
            android:enabled="false"
            android:text="Descargar, validar y activar mejor modelo"
            android:textAllCaps="false"
            android:textColor="@color/background"
            android:textStyle="bold" />
    </LinearLayout>
</ScrollView>
